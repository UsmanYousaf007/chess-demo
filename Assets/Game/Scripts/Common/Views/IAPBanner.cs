using UnityEngine;
using UnityEngine.UI;

public class IAPBanner : MonoBehaviour
{
    public string key;
    public Text price;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
